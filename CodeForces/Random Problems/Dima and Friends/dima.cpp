#include <bits/stdc++.h>
using namespace std;
#define MAXN 8  
int n, s;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    cin >> n;
    for(int i = 0, a; i < 7; i++)
        cin >> a, s += a;
    (n) 

    return 0;
}

// (s + x - 1) / (n + 1) !=  * k