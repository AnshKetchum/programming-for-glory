#include <bits/stdc++.h>
using namespace std;
#define MAXN 150000

int n,k,ans[MAXN],pos[MAXN],f[MAXN];
vector <int> eaten;
set <int> best_min;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    cin >> n >> k;
    for(int i = 0; i < n; i++)
    {
        cin >> f[i];
        pos[]
    }

    return 0;
}