#include <bits/stdc++.h>
using namespace std;

int a,b;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    cin >> a >> b;
    cout << ((b * b * b) / (a * a * a)) << endl;
    return 0;
}